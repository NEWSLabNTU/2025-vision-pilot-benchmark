---
# Master playbook to install and validate VisionPilot prerequisites (Revised)

- name: Install VisionPilot Prerequisites (Revised)
  import_playbook: install_prerequisites.yml
  tags:
    - install
    - prerequisites

- name: Download Models using standalone script
  import_playbook: models_download.yml
  tags:
    - models
    - download

# TensorRT compilation removed from default setup workflow
# Engines are compiled automatically at runtime on first inference
# To compile manually: make compile-tensorrt (uses compile_tensorrt.yml)

- name: Validate Installation
  import_playbook: validate_installation.yml
  tags:
    - validate